<h1>
    <%= @post.body %>
</h1>
    <ul>
        <% @comments.each do |comment| %>
            <li>
                <%= comment.body %>--
                <%= comment.user.name %>
            </li>
        <% end %>
    </ul>
    <%= link_to  "Add Comment", new_post_comment_path(@post)  %>